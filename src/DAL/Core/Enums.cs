using System;

namespace DAL.Core
{
    public enum Gender
    {
        None,
        Female,
        Male
    }

    public enum FileType
    {
        UNSPECIFIED = 0,
        IMAGE = 1,
        DOCUMENT = 2,
        PDF = 3,
    }
}
